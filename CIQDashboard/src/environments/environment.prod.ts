export const environment = {
  production: true,
  api: {
    auth: `${window.location.origin}/api/`,
    ciqdashboard :  `${window.location.origin}/api/idashboard/`
  },
  storagePrefix: 'idash-ui'
};